# Starlink GPS Helper

A minimal HTTPS page used by Starlink Dash to request the phone location and
return latitude, longitude, and accuracy directly to the local ESP32 page at
`http://192.168.4.1`.

The page does not contain firmware source code, passwords, STM32 commands, or
analytics. It does not store or transmit the selected coordinates to a server.
